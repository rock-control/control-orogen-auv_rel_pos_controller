# Generated from orogen/lib/orogen/templates/tasks/CMakeLists.txt

include(auv_rel_pos_controllerTaskLib)
ADD_LIBRARY(${AUV_REL_POS_CONTROLLER_TASKLIB_NAME} SHARED 
    ${AUV_REL_POS_CONTROLLER_TASKLIB_SOURCES})

add_dependencies(${AUV_REL_POS_CONTROLLER_TASKLIB_NAME}
    regen-typekit)


TARGET_LINK_LIBRARIES(${AUV_REL_POS_CONTROLLER_TASKLIB_NAME}
    ${OrocosRTT_LIBRARIES}
    ${AUV_REL_POS_CONTROLLER_TASKLIB_DEPENDENT_LIBRARIES})
SET_TARGET_PROPERTIES(${AUV_REL_POS_CONTROLLER_TASKLIB_NAME}
    PROPERTIES LINK_INTERFACE_LIBRARIES "${AUV_REL_POS_CONTROLLER_TASKLIB_INTERFACE_LIBRARIES}")

INSTALL(TARGETS ${AUV_REL_POS_CONTROLLER_TASKLIB_NAME}
    RUNTIME DESTINATION bin
    LIBRARY DESTINATION lib/orocos)

INSTALL(FILES ${AUV_REL_POS_CONTROLLER_TASKLIB_HEADERS}
    DESTINATION include/orocos/auv_rel_pos_controller)

